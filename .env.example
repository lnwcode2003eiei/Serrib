# Only needed when connecting the browser directly to a separately hosted backend.
# VITE_SERVER_URL=http://localhost:3000
# CLIENT_ORIGIN=http://localhost:5173,http://127.0.0.1:5173
# PORT=3000

# Vercel environment (set in dashboard, then redeploy):
# VITE_SERVER_URL=https://YOUR-SERVER.onrender.com
# Render environment (set in dashboard):
# CLIENT_ORIGIN=https://YOUR-SITE.vercel.app
# NODE_ENV=production
